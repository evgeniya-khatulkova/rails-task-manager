<h2>My tasks</h2>

<div class="container">
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task) %></li>
    <% end %>
  </ul>
</div>

<div class="task">
  <%= link_to task_new_path do%>
    <p>Add new task</p>
  <%end%>
</div>
